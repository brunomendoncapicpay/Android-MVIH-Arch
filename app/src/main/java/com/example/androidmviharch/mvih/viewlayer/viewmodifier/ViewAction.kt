package com.example.androidmviharch.mvih.viewlayer.viewmodifier

/**
 * Classe responsável por ações a serem realizadas na view uma única vez (sem manter estado)
 */
interface ViewAction : ViewModifier
